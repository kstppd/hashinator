name: C/C++ CI


on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: run_tests
      run: export TERM=xterm ; cd tests; bash -ic ./run_tests.sh
    
