name: C/C++ CI


on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: run_tests
      run:  cd tests; which nvcc ; zsh -c  ./run_tests.sh
      shell: zsh
    
